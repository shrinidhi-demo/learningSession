name: Print Hello World

on:
  push:
    branches:
      - main

jobs:
  print-hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello World
        run: |
          echo "Hello, World!"
